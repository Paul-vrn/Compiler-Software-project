class A {
   protected int x;
  int getX() {
          return x;
  }
  void setX(int x) {
        this.x = x;
   }
}

{
    A a = new A();
    int y;
    a.setX(1);
    println("a.getX=", a.getX());
    a.setX(2);
    println("a.getX=", a.getX());
    y = a.getX() + 3;
}