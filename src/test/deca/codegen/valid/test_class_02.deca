class A {
   protected int x;
   void setX(int x) {
        this.x = x;
   }
   int getX() {
        return x;
   }
}

{
    int x = 12;
    //A a = new A();
    //a.setX(1);
    //println("a.getX=", a.getX());
}