class A {
   protected int x;
  int getX() {
          return x;
  }
  void setX(int x) {
        this.x = x;
   }
}

{
    int x = 12;
    A a = new A();
    a.setX(1);
    println("a.getX=", a.getX());
}